export interface ISignUpModel {
    name: string;
    profilePic: string;
    email: string;
    password: string;
}

export interface ISignInModel {
    email: string;
    password: string;
}